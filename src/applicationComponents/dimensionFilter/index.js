import { connect } from 'react-redux';
import DimensionFilter from './dimensionFilter';

const mapStateToProps = (state) => {    
    return {};
};

export default connect(mapStateToProps, {  })(DimensionFilter);
