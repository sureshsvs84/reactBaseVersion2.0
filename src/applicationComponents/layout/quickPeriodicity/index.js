import { connect } from 'react-redux';
import QuickPeriodicity from './quickPeriodicity';

const mapStateToProps = (state) => {    
    return {};
};

export default connect(mapStateToProps, {  })(QuickPeriodicity);
